{
  "version": "0.2.0",
  "configurations": [
    {
   , should we repaet that every time , i sondt think "sourceMap": {},   "name": "Debug autodiff_SSMvector",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/autodiff_SSMvector",
      "args": [],
      "cwd": "${workspaceFolder}",
      "sourceLanguages": [
        "rust"
      ]
    }
  ]
}